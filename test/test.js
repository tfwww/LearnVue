var Seed = require('seed')

// describe('Seed', function () {
//     it('should have a variable', function () {
//         assert.equal(Seed, 'winterTest')
//     })
// })

describe('Seed', function () {
    it('should have a create method', function () {
        assert.ok(Seed.create)
    })
})